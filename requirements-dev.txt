# Test and eval tooling. Not installed in the API image.
#   pip install -r requirements-dev.txt

-r requirements.txt

pytest==9.1.1
httpx==0.28.1        # FastAPI's TestClient transport

# DeepEval powers tests/test_deepeval_reasoning.py.
#
# Must stay at 4.x. Version 1.1.9 declares its langchain dependencies with
# no upper bound, so a fresh install resolves langchain 1.x, where
# `langchain.schema` no longer exists — deepeval then fails to import at all.
deepeval==4.1.4
