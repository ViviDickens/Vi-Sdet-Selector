# Only needed for the optional AI layer.
# /predict works with none of these set.

# Powers POST /predict/explain (writes the natural-language reasoning)
ANTHROPIC_API_KEY=

# Powers tests/test_deepeval_reasoning.py (DeepEval's default judge model)
OPENAI_API_KEY=
