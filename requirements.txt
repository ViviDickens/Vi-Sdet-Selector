# Runtime dependencies — everything needed to serve the API.
# Test and eval tooling lives in requirements-dev.txt so it stays out of
# the production image (Dockerfile/Dockerfile.api).

fastapi==0.140.2
uvicorn[standard]==0.51.0
pydantic==2.13.4

# Backs POST /predict/explain only. Imported lazily in server/reasoning.py,
# so POST /predict works even if this is missing.
anthropic==0.120.0
