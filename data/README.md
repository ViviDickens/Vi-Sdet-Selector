# Data

This folder is dedicated to datasets used for training and testing.  
Typical files include:

- `train.csv`: Automatically collected dataset for model training.
- Other datasets required for experimentation and evaluation.

Datasets are generated by the scripts in the `/tools` folder.