# Data

No datasets here (anymore).

This folder originally held training data (`sample_train.csv`,
`schema.json`) for a scikit-learn model. Since the project moved to a
deterministic rule engine (`server/rules.py`), there's no training data
to store — the rules are hand-written, not learned.

Kept as a placeholder in case that changes.
