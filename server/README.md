# Server

This folder contains the FastAPI backend that exposes the `/predict` endpoint.  
It serves the trained ML model (`model.joblib`) and processes requests from the client.  
Here you will find the core application logic for predictions and API responses.