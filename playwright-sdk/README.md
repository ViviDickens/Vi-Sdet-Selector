# Playwright SDK

This folder contains the TypeScript integration with Playwright.

- **predict.ts**: Calls the FastAPI `/predict` endpoint and generates locators for Playwright tests.

This SDK is used to validate selectors dynamically during automated UI tests.