# Model

This folder contains the machine learning logic.

- **train.py**: Script to train the model.
- **evaluate.py**: Script to test and validate the model.

The trained model is serialized and stored as `model.joblib` (not committed by default).